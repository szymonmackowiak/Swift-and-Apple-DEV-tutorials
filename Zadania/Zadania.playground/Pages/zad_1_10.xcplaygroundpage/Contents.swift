/* Zadanie 1.10
 Napisz skrypt, który na podstawie zmiennych reprezentujących długości boków prostokąta obliczy ich obwód i pole.
 */

var firstSide: Float = 12
var secondSide: Float = 20

var perimeter = 2 * firstSide + 2 * secondSide
var area = firstSide * secondSide

print("Perimeter of the rectangle is: 2 * \(firstSide) + 2 * \(secondSide) = \(perimeter)")
print("Area of the rectangle is: \(firstSide) * \(secondSide) = \(area)")
